// Luiz Felipe Massaneiro - "Compartilhe o conhecimento"
/* Nessa aula, aprenderemos algumas definições a respeito dos Arrays, bem como,
diversas características que os definem e os fazem tão especiais. Inicialmente, temos
que um Array pode ser chamado de diversas maneiras, por exemplo, Array, Vetor, Tabela,
Matriz, tudo está se referindo a estrutura Array, porém, com nomes diferentes, mas que
fazem menção a uma mesma estrutura. Todo array é constituído por elementos, isto é, toda
estrutura Arrays contém uma série de elementos encadeados. Quando declaramos um Array,
devemos especificar, a quantidade de elementos que precisamos, assim, todo Array possui
a quantidade de elementos que o mesmo terá, ainda no momento da sua declaração. Todo Array
sempre possuirá um índice, bem como, cada elemento do Array, sempre terá um valor numérico,
não negativo associado. O índice, as vezes, também é chamado de subíndice.
*/


#include<iostream>
using namespace std;
using std::cout;
using std::cin;
using std::endl;
#include<stdlib.h>

int main() {





    return 0;


}
